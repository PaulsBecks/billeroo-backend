os: linux
services:
  - docker

before_install:
  - cd ./auth-backend && sh create_env_file.sh
  - cd .. && docker build -t paulsbecks/billeroo-backend -f auth-backend/Dockerfile.dev .

script:
  - docker run paulsbecks/billeroo-backend npm run test -- --coverage
